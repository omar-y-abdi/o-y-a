name: ChatGPT CMS usability bridge

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  idle:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Transport branch parked while exact patch is uploaded"
